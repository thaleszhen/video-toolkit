# Sample Media

Local smoke-test media files are kept in this folder to keep the project root clean.

Current local sample path:

- `samples/smoke-input.mp4`

Notes:

- Media files (`*.mp4`) are ignored by git in this project.
- This folder is for local/manual verification assets only.